\usepackage{natbib}

This is a simple text written with a text editor and layouted with the
{\LaTeX} system as described by \citet{lamport94}.

% Also the bibliography style 
%  needs to be changed:
\bibliographystyle{plainnat}
\bibliography{bibtex-library}
