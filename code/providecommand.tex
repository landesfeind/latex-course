% This will fail, because the command already exists:
%\newcommand{\emph}[1]{ {\color{red}#1} }

% Alternatively:
\providecommand{\emph}[1]{ {\color{red}#1} }

It is important to \emph{listen} to you instructor.
